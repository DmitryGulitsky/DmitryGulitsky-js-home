describe('Object x2 function', () => {
  it ('Test final object, if only numbers inside'), ()=> {
    expect(multiplyNumeric(image = {width: 100, height: 400, title: 'Cool image'}))
  .toBe(image = {width: 200, height: 800, title: 'Cool image'});
  }
})
